# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code workspace state (local only)
.claude/

# Supabase CLI scratch state (local only)
supabase/.temp/

# Java / Maven build artifacts
**/target/
*.class
# Stray compiled output accidentally written to service roots
backend/*/com/
backend/*/org/
backend/*/META-INF/

# Flutter/Dart build artifacts (also covered by nested .gitignore files)
**/.dart_tool/
**/build/
*.iml

# Infra env (already covered by .env, listed for clarity — never commit secrets)
infra/.env
